{
  "output": "skills/code-review-and-quality/SKILL.md",
  "source": "skills/code-review-and-quality/SKILL.md",
  "classification": "mdf-overlay-required",
  "overlay": null,
  "artifactStorageOverride": false,
  "notes": "MDF adds artifact storage, supplied MDF artifact context, human-facing language, and severity-label adapter guidance while preserving the upstream review method.",
  "baseSha256": "6d7c7cb43e8eb6727ab72b3baa570d8e8c00f484392cfd07acf4550fed2b6838",
  "overlayKind": "patch",
  "exactPatches": [
    {
      "id": "mdf-review-artifact-storage-and-language",
      "search": "# Code Review and Quality\n\n## Overview",
      "replace": "# Code Review and Quality\n\nWhen saving a code review report, verify MDF user and project init state, resolve the current MDF work item, and write `.mdf/work/{work_id}/review-NNN.md`. If init state is missing, stop and instruct the user to run `mdf init`. Repeated saves create new revisions and update `item.md` `latest.review` plus `.mdf/index.jsonl`.\n\nBefore writing MDF review findings, explanations, or recommendations, follow `../../references/human-facing-language.md`. Use the explicit `human_language` preference for human-facing prose while preserving fixed labels, file paths, code identifiers, commands, and MDF artifact paths.\n\n## Overview"
    },
    {
      "id": "mdf-supplied-artifact-context",
      "search": "Review covers five axes: correctness, readability, architecture, security, and performance.",
      "replace": "Review covers five axes: correctness, readability, architecture, security, and performance. MDF-managed reviews may also use task cards, spec artifacts, plan artifacts, build artifacts, review artifacts, and acceptance criteria supplied by the calling workflow as review context; those artifacts provide scope and evidence, but the technical review method remains the five-axis review below."
    },
    {
      "id": "mdf-context-step-artifacts",
      "search": "```\n- What is this change trying to accomplish?\n- What spec or task does it implement?\n- What is the expected behavior change?\n```\n\n### Step 2: Review the Tests First",
      "replace": "```\n- What is this change trying to accomplish?\n- What spec or task does it implement?\n- What is the expected behavior change?\n```\n\nFor MDF-managed work, use caller-supplied `.mdf/work/{work_id}/` artifacts and task criteria as context when they are available. Do not invent missing MDF artifacts or broaden the requested review scope just because the repository contains other workflow state.\n\n### Step 2: Review the Tests First"
    },
    {
      "id": "mdf-severity-label-compatibility",
      "search": "| **FYI** | Informational only | No action needed — context for future reference |\n\nThis prevents authors from treating all feedback as mandatory and wasting time on optional suggestions.",
      "replace": "| **FYI** | Informational only | No action needed — context for future reference |\n\nWhen an MDF caller expects severity labels `Critical`, `Important`, and `Suggestion`, keep upstream semantics: map `Critical` to `Critical`, required no-prefix findings to `Important`, and `Optional` / `Consider` to `Suggestion`. Preserve fixed labels requested by the caller, but do not weaken required findings into optional ones.\n\nThis prevents authors from treating all feedback as mandatory and wasting time on optional suggestions."
    }
  ],
  "normalizedDifferences": "Rendered from the pinned upstream source with focused MDF adapter patches for review artifact storage, human-facing-language handling, caller-supplied MDF context, and severity-label compatibility. The previous full replacement duplicated MDF build/review protocol sections and omitted upstream structural-review guidance; this migration restores upstream review philosophy, five-axis criteria, structural remedies, change sizing, review checklist, red flags, and presumptive blockers."
}
